openai>=1.30.0
pyyaml>=6.0
pydantic>=2.0
python-dotenv>=1.0.0
requests>=2.31.0
tenacity>=8.2.0
rich>=13.0.0

# Roadmap notes (kept as comments so pip can parse this file):
# The multi-agent system is intended to combine:
#   - an LLM agent that checks resumes for a CRN, plus a suggestion system
#   - an image model agent that renders schedule models/simulations and interactive views
