openai>=1.30.0
pyyaml>=6.0
pydantic>=2.0
python-dotenv>=1.0.0
requests>=2.31.0
tenacity>=8.2.0
rich>=13.0.0

notes: Multi agent system can be between
-  LLM which checks resumes for CRN, + Suggestion system
-  image model which shows schedule models and simulations + interactive models
